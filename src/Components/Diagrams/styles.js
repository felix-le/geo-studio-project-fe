import { makeStyles } from '@mui/styles';

const useStyles = makeStyles((theme) => ({
  root: {},

  section: {
    padding: '60px 0',
  },
  heading: {
    fontSize: '2rem',
    fontWeight: 'bold',
    textAlign: 'center',
  },
  tabWrapper: {},
}));

export { useStyles };
